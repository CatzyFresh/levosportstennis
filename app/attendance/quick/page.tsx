"use client";import { useState } from "react";import { useAppStore } from "@/store/useAppStore";
export default function(){const s=useAppStore(); const [playerId,setPlayerId]=useState(s.players[0]?.id||''); const [status,setStatus]=useState('present'); return <div className="space-y-2"><h2 className="text-xl font-semibold">Quick Attendance Marking</h2><select className="bg-slate-900 border p-2" value={playerId} onChange={e=>setPlayerId(e.target.value)}>{s.players.map(p=><option key={p.id}>{p.id}</option>)}</select><select className="bg-slate-900 border p-2 ml-2" value={status} onChange={e=>setStatus(e.target.value)}><option>present</option><option>absent</option><option>late</option><option>excused</option></select><button className="ml-2 bg-cyan-600 px-2 py-1 rounded" onClick={()=>s.addAttendance({id:`A-${Date.now()}`,date:new Date().toISOString().slice(0,10),playerId,status:status as any})}>Mark</button></div>}
