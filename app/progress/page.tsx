"use client";import { useAppStore } from "@/store/useAppStore";
export default function(){const s=useAppStore();return <div><h2 className="text-xl">Player Progress</h2>{s.players.map(p=>{const reviews=s.performanceReviews.filter(r=>r.playerId===p.id);const avg=reviews.length?(reviews.reduce((a,b)=>a+b.technical+b.tactical+b.mental,0)/(reviews.length*3)).toFixed(1):'-';return <div key={p.id}>{p.fullName}: avg core rating {avg}</div>})}</div>}
