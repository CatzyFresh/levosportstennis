"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { useAppStore } from "@/store/useAppStore";
export default function(){const [name,setName]=useState('');const {addCoach}=useAppStore();const r=useRouter();return <form onSubmit={e=>{e.preventDefault();addCoach({id:`C-${Date.now()}`,name,active:true});r.push('/coaches')}}><h2 className="text-xl">Create Coach</h2><input className="bg-slate-900 border p-2 rounded" value={name} onChange={e=>setName(e.target.value)}/><button className="ml-2 bg-cyan-600 px-2 py-1 rounded">Save</button></form>}
