"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { useAppStore } from "@/store/useAppStore";
export default function(){const [name,setName]=useState('');const s=useAppStore();const r=useRouter();return <form onSubmit={e=>{e.preventDefault();s.addTournament({id:`T-${Date.now()}`,name,startDate:new Date().toISOString().slice(0,10),surface:'Hard'});r.push('/tournaments')}}><h2 className="text-xl">Create Tournament</h2><input className="bg-slate-900 border p-2" value={name} onChange={e=>setName(e.target.value)}/><button className="ml-2 bg-cyan-600 px-2 py-1 rounded">Save</button></form>}
